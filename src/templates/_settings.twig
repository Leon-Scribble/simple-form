{# @var plugin \scribblewerbeagentur\craftsimpleform\SimpleForm #}
{# @var settings \scribblewerbeagentur\craftsimpleform\models\Settings #}

    {% import '_includes/forms.twig' as forms %}
    {{ forms.textField({
        first: true,
        label: 'Receiver',
        name: 'receiver',
        value: settings.receiver
    }) }}

    {{ forms.textField({
        label: 'Subject',
        name: 'subject',
        value: settings.subject
    }) }}
    {{ forms.lightswitch({
        label: 'Override Copy E-Mail',
        name: "overrideCopyEmail",
        value: settings.overrideCopyEmail
    }) }}


{# ... #}
